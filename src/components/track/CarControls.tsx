import Button from '../../ui/buttons/Button.tsx';

const CarControls = () => (
  <div className="car-controls">
    <Button className={'car-controls-start'} text={'a'} />
    <Button className={'car-controls-stop'} text={'b'} />
  </div>
);

export default CarControls;
